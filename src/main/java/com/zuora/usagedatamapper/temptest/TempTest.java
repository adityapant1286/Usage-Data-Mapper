package com.zuora.usagedatamapper.temptest;

import com.zuora.usagedatamapper.model.configs.vo.Schedule;

import java.util.UUID;

public class TempTest {
    private final static UUID uuid = UUID.randomUUID();
    public static void main(String[] args) {
//        System.out.println(String.format("Not found by %s", "id"));
//        Schedule schedule = Schedule.builder().configId(uuid).cronString("? ? ? ? ? ?").build();


    }
}
